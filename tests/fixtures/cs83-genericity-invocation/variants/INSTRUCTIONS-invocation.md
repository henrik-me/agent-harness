# INSTRUCTIONS (leaked an invocation — must FAIL on the invocation scan)

Bootstrap the workspace by running node bin/harness.mjs startup.

<!-- harness:local-start id=instructions.harness -->
_(Project-local orchestration notes. Empty by default.)_
<!-- harness:local-end id=instructions.harness -->

Generic onboarding prose with no harness-internal anchor references.
