# REVIEWS (composed process base)

Review doctrine. No harness-repo run commands here; consumers invoke the CLI via
the `{{harness_invoke}}` placeholder. Source refs such as `bin/harness.mjs` are
prose only.
