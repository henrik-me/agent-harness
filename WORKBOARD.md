# Work Board

Live coordination file for multi-agent work. Only orchestrating agents update this file.

> **Last updated:** 2026-07-06 (yoga-ah-c2 — **CS90b CLOSED**: shipped **#392 / L3** (ADR-0005) — `pr_check.mode ∈ {lint+drift (default), drift-only}` (schema-first) + a **BASE-tree** mode read (fail-safe to `lint+drift`; never PR head, CS63 R13) so `drift-only` skips the redundant `harness lint` while keeping the managed-drift classifier + `harness-managed-edit-ack` escape valve; **Q1 resolved** — a **report-only** `harness sync` adoption-overlap advisory (exported `detectPrCheckLintOverlap()`, self-host-silent, warn-only). 20 tests. Implemented by sub-agent `cs90b-impl` (`claude-opus-4.8`). Content **PR #526** (`47c614b`) admin-squash-merged (CS91 Rec A) after **gpt-5.5 rubber-duck Go** (`cs90b-review`) + **Copilot COMMENTED** (3 nits fixed, threads resolved; loop **bounded per LRN-221**). Branch was BEHIND (sibling **CS113**/**LRN-222/223** advanced main — orthogonal; LRN-223 stale-base); `read-only-gates` rerun after evidence landed (LRN-194/217). **PVI GO** (`cs90b-pvi`, 6/6 + 5/5). **#392 auto-closed.** No SemVer bump (`[Unreleased]`). `harness lint` 43/0/3, `node --test` +20, `sync --mode=check` no-drift. See CONTEXT.md for detail.) — prior: 2026-07-05 (yoga-ah — **CS90a CLOSED**: shipped **#391 / L1** (ADR-0005) — consumer guide **`docs/ci-drift-layering.md`** with the canonical hardened L1 per-PR `sync --mode=check` drift gate (npx + clone-then-run; leading-alphanumeric ref anchor + `git checkout --detach` fail-closed; SHA-pinned, `contents: read`) + **`harness-drift.yml` (L2)** header repositioned as low-activity belt-and-suspenders (behaviour unchanged) + L1-vs-L3 drift-semantics note. Content **PR #516** (`27fcc3e`) admin-squash-merged: gpt-5.5 R1–R11 all Go (2 rebases onto the CS109a base → narrow re-attests R7/R10; R8 case-quote, R11 `--detach`) + Copilot COMMENTED converged (2 SHA-pin findings **FALSE POSITIVE** — `de0fac2e` **is** checkout v6.0.2; **LRN-221**). PVI **GO** by `gpt-5.5` (5/5 deliverables). **#391 closed.** Patch. `harness lint` 43/0/3, `node --test` 2049 pass/5 skip. See CONTEXT.md for detail.) — prior: 2026-07-05 (yoga-ah-c2 — **CS109a CLOSED**: shipped the deferred half of CS109 (ADR-0006 § D6) — the live-ruleset mutation verb `harness ruleset apply` (dry-run) **+ `apply --apply`** (fail-closed live GitHub-API PUT; binding pre-apply preflight [`sync --mode=check` clean + zero F3 `ruleset-deadlock`] + mandatory post-apply verify). Content **PR #517** (`2cd5d1a`) shipped the verb; the PVI close-out review caught a **GitHub-API-shape defect** (the diff + PUT body used internal `required_checks` strings, but the API uses `required_status_checks` objects) → fast-follow **PR #518** (`5acebaf`, `gpt-5.5` R1 Go + Copilot COMMENTED, admin-squash-merged) made the diff read both shapes + serialize the PUT to API shape (renderer/source untouched → sync drift byte-identical). Self-host posture flip **DEFERRED** — **G109-ruleset-apply** not granted → **CS106** owns the concrete required-check/count-0 flip (verb-only per Decision 4). PVI **GO** by `gpt-5.5` (`cs109a-pvi-review`, 6/6 deliverables). Re-hit LRN-197/210/211 (single-call `harness review` Go-row/Reviewer-agent glitches — fixed by hand). `harness lint` 43/0/3, `node --test` 2049 pass/5 skip. See CONTEXT.md for full detail.) — prior: 2026-07-05 (yoga-ah — **CS107 CLOSED**: cut harness release **v0.17.0** (Minor) bundling the 9 `[Unreleased]` CSs merged since v0.16.0 (CS24/26/68/71/75/76/86/87/91). file **#489** → claim **#490** → content **PR #492** (`5580087`) admin-squash-merged (gpt-5.5 R1 Go + Copilot COMMENTED; async `read-only-gates`/`copilot-review-attached` re-run green after the Copilot review landed — LRN-194; A3/A4 Model-audit agent-identity fixed by hand `yoga-ah`→`rubber-duck` — LRN-197). **Phase B:** annotated tag `v0.17.0` on the squash SHA + Release **published to Latest** (`--no-draft`). Consumers notified issue-only (**sub-invaders#152**, **authzandentitlements#149**). #229/#394 ship already-closed; **#395** stays OPEN (Rec A/C shipped; Rec B → CS106). PVI **GO** by `gpt-5.5` (8/8 deliverables). `harness lint` 37/0/3, `node --test` 1947 pass/5 skip. See CONTEXT.md for full detail.) — prior: 2026-07-04 (yoga-ah — **CS26 CLOSED**: `harness init` improvements bundle (four findings #2/#3/#6/#9 from the CS16 bootstrap; #4/#5 obsolete). #2 normalized real `version` pin (`normalizeInitVersion`); #3 new `check-config-placeholders` linter (cmdLint, JSON-aware, skips `_comment`); #6 deleted the stray root + `.github` `.gitkeep` seeds (clickstops retained); #9 seeded `.gitattributes`. Content **PR #471** (`533d8bb`) admin-squash-merged (GPT-5.5 R1-R4 Go + Copilot ×3 nits fixed; rebased onto main around CS24). Issue **#146** reconciled (plan R6, Option A): a fresh init is structurally lint-clean via `harness lint --skip config-placeholders` + the linter flags the unfilled config; sync-gate alternative (Option C) noted for maintainer review. PVI **GO** at `533d8bb` (8/8 deliverables). **LRN-202 filed** (open). `harness lint` 36/0/3, `node --test` 1836 pass. See CONTEXT.md for full detail.)

## Orchestrators

Status vocabulary: `🟢 Active` (Last Seen within 24h), `🟡 Idle` (24h-7d), `⚪ Offline` (>7d). Agent-ID derivation per [TRACKING.md § Agent Identification](TRACKING.md#agent-identification).

| Agent ID | Machine | Repo Folder | Status | Last Seen |
|----------|---------|-------------|--------|-----------|
| omni-ah  | HENRIKM-OMNI | C:\src\agent-harness | 🟢 Active | 2026-06-24 |
| omni-ah-c2 | HENRIKM-OMNI | C:\src\agent-harness_copilot2 | 🟢 Active | 2026-07-02 |
| omni-ah-c3 | HENRIKM-OMNI | C:\src\agent-harness_copilot3 | 🟢 Active | 2026-07-02 |
| yoga-ah  | HENRIKM-YOGA | C:\src\agent-harness | 🟢 Active | 2026-07-04 |
| yoga-ah-c2 | HENRIKM-YOGA | C:\src\agent-harness_copilot2 | 🟢 Active | 2026-06-06 |
| yoga-ah-c3 | HENRIKM-YOGA | C:\src\agent-harness_copilot3 | 🟢 Active | 2026-06-08 |

## Active Work

<!--
Active Work table accepts two empty forms (see template/seeded/WORKBOARD.md):
  (a) header-only — keep header + separator rows ONLY (form below)
  (b) em-dash placeholder — single data row with "no active CS" in Title cell
Do NOT use `_(none)_` placeholder rows — `check-workboard.mjs` will reject them.
-->

| CS-Task ID | Title | State | Owner | Branch | Last Updated | Blocked Reason |
|------------|-------|-------|-------|--------|--------------|----------------|
| — | no active CS | — | — | — | — | — |

> **Note:** WORKBOARD shows live coordination state only — active orchestrators and their active work. The queue lives in `project/clickstops/planned/` (priority via filename + per-file `**Depends on:**`); historical record lives in `project/clickstops/done/`. Do not duplicate either here.
