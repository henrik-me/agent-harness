# CS11 sync probe report

> Generated by `node bin/harness.mjs sync --dry-run --report --cwd .`
> against `cs11/content` after `harness.config.json` lands.
>
> **Expected: nonzero exit with composed-merge error.** This is documented
> in CS11 Stage A.3: current root composed docs (CONVENTIONS, OPERATIONS,
> REVIEWS) lack the local-block markers required by `mergeComposed`.
> Stage B.1's manual render-and-write strategy bypasses this — see D7 in
> the active CS file. The probe value is confirming this expected failure
> mode, NOT generating a per-file preview (that's `render-preview.md`).

## Probe output

```text
Sync error: Composed merge failed for "CONVENTIONS.md": Consumer file contains content outside local blocks that does not match the template. Provide legacy_composed_mapping.json to map or discard each legacy region.
```
